services:
  postgres:
    container_name: relay_pg
    image: postgres:18.6-alpine3.24
    env_file:
      - .env
    ports:
      - 5433:5432
    volumes:
      - postgres_data:/var/lib/postgresql/18
    
volumes:
  postgres_data: